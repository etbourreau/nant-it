#!/bin/sh
 
ssh app@5.135.240.8 <<EOF
  cd /nant-it
  git pull
  cd /nant-it/web
  sudo fuser -k 80/tcp
  sudo npm start
  cd /nant-it/db
  sudo fuser -k 3000/tcp
  sudo npm start
 # npm install --production
 # forever restartall
  exit
EOF
